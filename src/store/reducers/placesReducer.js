const initialState = {
    placeName: "",
    places: [],
    selectedPlace: null
}

const PlaceReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default PlaceReducer